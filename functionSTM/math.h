#ifndef MATH.H
#define MATH.H

/* These are also calls as exposed function prototypes */
int math_add(int n1, int n2);
long long int math_mul(int n1, int n2);
float math_dic(int n1, int n2);

#endif /* MATH_H_ */
